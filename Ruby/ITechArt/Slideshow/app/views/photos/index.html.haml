%h1 Listing photos

%table.table.table-striped
  %thead
    %tr
      %th= 'Photo_id'
      %th= 'Actions'
      %th
      
  %tbody
    - @photos.each do |photo|
      %tr
        %td= photo.id
        %td= link_to 'Show', photo
        %td= link_to 'Edit', edit_photo_path(photo)
        %td= link_to 'Destroy', photo, :method => :delete

%br

= link_to 'New Photo', new_photo_path
